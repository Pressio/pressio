

This page explains the API for using the pressio Galerkin ROMs. After reading this, you should understand what a \char`\"{}pressio Galerkin problem\char`\"{} is, the variants we currently support, and how to use the problem after instantiating it.

If anything is unclear, and/or you have suggestions on how to improve this page, \href{https://github.com/Pressio/pressio/issues}{\texttt{ open an issue on github}}. 

~\newline
\hypertarget{md_pages_components_rom_galerkin_autotoc_md90}{}\doxysection{Everything starts with creating a problem!}\label{md_pages_components_rom_galerkin_autotoc_md90}
The main entry point to use the pressio Galerkin ROMs is the problem class. You create an instance of one of the supported \char`\"{}\+Galerkin problems\char`\"{} as\+:



\begin{DoxyParagraph}{}

\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{namespace }pgal    = pressio::rom::galerkin;}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keyword}{auto} scheme = pressio::ode::StepScheme::some\_value;}
\DoxyCodeLine{\textcolor{keyword}{auto} problem      = pgal::create\_<keywords>(scheme, \textcolor{comment}{/* args */} );}

\end{DoxyCode}
 
\end{DoxyParagraph}


where {\ttfamily $<$keywords$>$} express the variant you want (more below), {\ttfamily some\+\_\+value} is an enum value to select the time stepping scheme (e.\+g., explicit Euler, RK4, BDF1, etc), and {\ttfamily args} are the arguments needed which depend on the variant you choose.

We currently offer the following variants\+:

 \href{md_pages_components_rom_galerkin_default.html}{\texttt{ \enskip{}\quad{}Default Problem\quad{} \enskip{}  click to learn more }}

 \href{md_pages_components_rom_galerkin_hypred.html}{\texttt{ Hyper-\/reduced Problem   click to learn more }}

 \href{md_pages_components_rom_galerkin_masked.html}{\texttt{ \enskip{}\quad{} Masked Problem\enskip{}\quad{}  click to learn more }}\hypertarget{md_pages_components_rom_galerkin_autotoc_md91}{}\doxysection{Problem class API}\label{md_pages_components_rom_galerkin_autotoc_md91}
A Galerkin problem exposes different methods depending on whether you choose an explicit of implicit scheme. Below we discuss both scenarios.\hypertarget{md_pages_components_rom_galerkin_autotoc_md92}{}\doxysubsection{Explicit case}\label{md_pages_components_rom_galerkin_autotoc_md92}
If you create an explicit Galerkin problem, the problem exposes the following API\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{class }ExplicitGalerkinProblemClass}
\DoxyCodeLine{\{}
\DoxyCodeLine{\textcolor{keyword}{public}:}
\DoxyCodeLine{  \textcolor{keyword}{using} traits = \textcolor{comment}{/* nested typedef to access the problem's traits */};}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{comment}{// const ref to the object knowing how to reconstruct a FOM state}}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keyword}{auto} \& fomStateReconstructor() \textcolor{keyword}{const};}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{keyword}{template} <\textcolor{keyword}{class} TimeType, \textcolor{keyword}{class} StepCount>}
\DoxyCodeLine{  \textcolor{keywordtype}{void} operator()(galerkin\_state\_type \& state,}
\DoxyCodeLine{                  \textcolor{keyword}{const} TimeType current\_time,}
\DoxyCodeLine{                  \textcolor{keyword}{const} TimeType time\_step\_size\_to\_use,}
\DoxyCodeLine{                  \textcolor{keyword}{const} StepCount step\_count);}
\DoxyCodeLine{\};}

\end{DoxyCode}




\begin{DoxyParagraph}{Main thing to remember\+:}
An explicit Galerkin problem satisfies the \href{md_pages_components_ode_advance.html}{\texttt{ steppable concept}} (specifically, behaves like an \href{md_pages_components_ode_steppers_explicit.html}{\texttt{ explicit stepper}}). 
\end{DoxyParagraph}


If needed, the traits class contains\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::fom\_system\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::scalar\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::decoder\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::decoder\_jac\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::galerkin\_state\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::galerkin\_velocity\_type;}

\end{DoxyCode}
\hypertarget{md_pages_components_rom_galerkin_autotoc_md93}{}\doxysubsection{Implicit case}\label{md_pages_components_rom_galerkin_autotoc_md93}
If you create an implicit Galerkin problem, the problem exposes the following API\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{class }ImplicitGalerkinProblemClass}
\DoxyCodeLine{\{}
\DoxyCodeLine{\textcolor{keyword}{public}:}
\DoxyCodeLine{  \textcolor{keyword}{using} traits = \textcolor{comment}{/* nested typedef to access the problem's traits */};}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{keyword}{using} scalar\_type    = \textcolor{keyword}{typename} traits::scalar\_type;}
\DoxyCodeLine{  \textcolor{keyword}{using} state\_type     = \textcolor{keyword}{typename} traits::galerkin\_state\_type;}
\DoxyCodeLine{  \textcolor{keyword}{using} residual\_type  = \textcolor{keyword}{typename} traits::galerkin\_residual\_type;}
\DoxyCodeLine{  \textcolor{keyword}{using} jacobian\_type  = \textcolor{keyword}{typename} traits::galerkin\_jacobian\_type;}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{comment}{// const ref to the object knowing how to reconstruct a FOM state}}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keyword}{auto} \& fomStateReconstructor() \textcolor{keyword}{const};}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{keyword}{template} <\textcolor{keyword}{class} TimeType, \textcolor{keyword}{class} StepCount, \textcolor{keyword}{class} SolverType>}
\DoxyCodeLine{  \textcolor{keywordtype}{void} operator()(galerkin\_state\_type \& state,}
\DoxyCodeLine{                  \textcolor{keyword}{const} TimeType current\_time,}
\DoxyCodeLine{                  \textcolor{keyword}{const} TimeType time\_step\_size\_to\_use,}
\DoxyCodeLine{                  \textcolor{keyword}{const} StepCount step\_count,}
\DoxyCodeLine{                  SolverType \& solver)}
\DoxyCodeLine{  \{}
\DoxyCodeLine{    \textcolor{comment}{// NOTE: here we reveal a bit of how operator() works.}}
\DoxyCodeLine{    \textcolor{comment}{// This is on purpose so that some things below will be more clear.}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// The operator() has 3 main stages:}}
\DoxyCodeLine{    \textcolor{comment}{// 1. we prepare for doing a step by setting/updating what is needed}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// 2. call solver (which will query operators)}}
\DoxyCodeLine{    solver.solve(*\textcolor{keyword}{this}, state);}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// 3. we do some other things to end the step}}
\DoxyCodeLine{  \}}
\DoxyCodeLine{}
\DoxyCodeLine{  residual\_type createResidual() \textcolor{keyword}{const};}
\DoxyCodeLine{  jacobian\_type createJacobian() \textcolor{keyword}{const};}
\DoxyCodeLine{  \textcolor{keywordtype}{void} residual(\textcolor{keyword}{const} state\_type\& x, residual\_type \& res) \textcolor{keyword}{const};}
\DoxyCodeLine{  \textcolor{keywordtype}{void} jacobian(\textcolor{keyword}{const} state\_type\& x, jacobian\_type \& jac) \textcolor{keyword}{const};}
\DoxyCodeLine{\};}

\end{DoxyCode}




\begin{DoxyParagraph}{Main thing to remember\+:}
An implicit Galerkin problem satisfies the \href{md_pages_components_ode_advance.html}{\texttt{ steppable concept}} (specifically, behaves like an \href{md_pages_components_ode_steppers_implicit.html}{\texttt{ implicit stepper}}). 
\end{DoxyParagraph}


The traits contain\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::fom\_system\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::scalar\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::decoder\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::decoder\_jac\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::galerkin\_state\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::galerkin\_residual\_type;}
\DoxyCodeLine{\textcolor{keyword}{typename} traits::galerkin\_jacobian\_type;}

\end{DoxyCode}


~\newline
 \DoxyHorRuler{0}
 ~\newline
\hypertarget{md_pages_components_rom_galerkin_autotoc_md94}{}\doxysection{How do I use an EXPLICIT problem?}\label{md_pages_components_rom_galerkin_autotoc_md94}
See the following\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{namespace }pode = pressio::ode;}
\DoxyCodeLine{\textcolor{keyword}{namespace }pgal = pressio::rom::galerkin;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keyword}{auto} scheme = pdoe::StepScheme:ForwardEuler;}
\DoxyCodeLine{\textcolor{keyword}{auto} problem      = pgal::create\_default\_explicit\_problem(scheme, \textcolor{comment}{/* args */});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// we assume a galState exists}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// you can take a single step of the problem}}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keywordtype}{double} currentTime = 0.;}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.5;}
\DoxyCodeLine{problem(galState, currentTime, dt, 1);}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can define your own stepping loop}}
\DoxyCodeLine{\textcolor{keywordtype}{double} currentTime = 0.;}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.1;}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keywordtype}{int} step = 1; step <= 10; step++)\{}
\DoxyCodeLine{ problem(galState, currentTime, dt, 1);}
\DoxyCodeLine{ currentTime += dt;}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can use our own functions to advance the problem}}
\DoxyCodeLine{pode::advance\_n\_steps(problem, \textcolor{comment}{/* args */});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or}}
\DoxyCodeLine{pode::advance\_n\_steps\_and\_observe(problem, \textcolor{comment}{/* args */});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or others}}

\end{DoxyCode}


~\newline
\hypertarget{md_pages_components_rom_galerkin_autotoc_md95}{}\doxysection{How do I use an IMPLICIT problem?}\label{md_pages_components_rom_galerkin_autotoc_md95}
Recall that doing implicit time stepping it is not as simple as explicit. \href{md_pages_components_ode_steppers_implicit.html}{\texttt{ For implicit, in fact, you also need a {\itshape solver} to compute the solution at the next step}}. In the case of Galerkin, you can use a Newton-\/\+Raphson solver, because at eaach step, you are solving a (reduced) system of equations with as many equations as the number of unknowns. More specifically, the system you need to solve has as many equations as the dimensionality of your approximating subspace. See some sample snippets below\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{namespace }psolvers = pressio::nonlinearsolvers;}
\DoxyCodeLine{\textcolor{keyword}{namespace }pode     = pressio::ode;}
\DoxyCodeLine{\textcolor{keyword}{namespace }pgal     = pressio::rom::galerkin;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keyword}{auto} scheme = pdoe::StepScheme:BDF1;}
\DoxyCodeLine{\textcolor{keyword}{auto} problem      = pgal::create\_default\_implicit\_problem(scheme, \textcolor{comment}{/* args */});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// we assume a galState exists}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// for example, using the pressio solver}}
\DoxyCodeLine{\textcolor{keyword}{auto} solver = psolvers::create\_newton\_rapshon(problem, \textcolor{comment}{/* args */});}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// you can take a single step of the problem}}
\DoxyCodeLine{\{}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} currentTime = 1.5;}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.5;}
\DoxyCodeLine{  problem(galState, currentTime, dt, 1, solver);}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can define your own stepping loop}}
\DoxyCodeLine{\{}
\DoxyCodeLine{  \textcolor{keywordtype}{double} currentTime = 0.;}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.1;}
\DoxyCodeLine{  \textcolor{keywordflow}{for} (\textcolor{keywordtype}{int} step = 1; step <= 10; step++)\{}
\DoxyCodeLine{   problem(galState, currentTime, dt, 1, solver);}
\DoxyCodeLine{   currentTime += dt;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can use our own functions to advance the problem}}
\DoxyCodeLine{pode::advance\_n\_steps\_and\_observe(problem, \textcolor{comment}{/* args */}, solver);}

\end{DoxyCode}
\hypertarget{md_pages_components_rom_galerkin_autotoc_md96}{}\doxysubsection{Using a custom solver}\label{md_pages_components_rom_galerkin_autotoc_md96}
In the snippet above, we show how to use the pressio Newton-\/\+Raphson solver to solve the Galerkin problem. If you want to use your own solver, you can do that! Here we discuss how.


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{template}<\textcolor{keyword}{class} R\_type, \textcolor{keyword}{class} J\_type>}
\DoxyCodeLine{\textcolor{keyword}{class }CustomSolver}
\DoxyCodeLine{\{}
\DoxyCodeLine{\textcolor{keyword}{private}:}
\DoxyCodeLine{  R\_type m\_R;}
\DoxyCodeLine{  J\_type m\_J;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{public}:}
\DoxyCodeLine{  \textcolor{keyword}{template}<\textcolor{keyword}{class} ProblemType>}
\DoxyCodeLine{  CustomSolver(ProblemType \& system)}
\DoxyCodeLine{    : m\_R(system.createResidual()),}
\DoxyCodeLine{      m\_J(system.createJacobian())}
\DoxyCodeLine{  \{\}}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{keyword}{template}<\textcolor{keyword}{class} ProblemType, \textcolor{keyword}{class} StateType>}
\DoxyCodeLine{  \textcolor{keywordtype}{void} solve(ProblemType \& problem, StateType \& state)}
\DoxyCodeLine{  \{}
\DoxyCodeLine{    \textcolor{comment}{// the problem object is the instance of the Galerkin problem you chose.}}
\DoxyCodeLine{    \textcolor{comment}{// You can compute residual, Jacobian, etc and solve as you wish}}
\DoxyCodeLine{    \textcolor{comment}{// for instance, you can do:}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{keywordflow}{for} (\textcolor{comment}{/* some loop or whatever your solver needs */})}
\DoxyCodeLine{    \{}
\DoxyCodeLine{      problem.residual(state, m\_R);}
\DoxyCodeLine{      problem.jacobian(state, m\_J);}
\DoxyCodeLine{      \textcolor{comment}{// do something with the residual (m\_R) and Jacobian (m\_J)}}
\DoxyCodeLine{    \}}
\DoxyCodeLine{  \}}
\DoxyCodeLine{\};}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{namespace }pode     = pressio::ode;}
\DoxyCodeLine{\textcolor{keyword}{namespace }pgal     = pressio::rom::galerkin;}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{const} \textcolor{keyword}{auto} scheme = pdoe::StepScheme:BDF1;}
\DoxyCodeLine{\textcolor{keyword}{auto} problem      = pgal::create\_default\_implicit\_problem(scheme, \textcolor{comment}{/* args */});}
\DoxyCodeLine{\textcolor{keyword}{using} traits      = \textcolor{keyword}{typename} decltype(problem)::traits:}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// instantiate your own solver}}
\DoxyCodeLine{\textcolor{comment}{// and you can use it like shown above}}
\DoxyCodeLine{using gal\_R\_type  = typename traits::galerkin\_residual\_type;}
\DoxyCodeLine{\textcolor{keyword}{using} gal\_J\_type  = \textcolor{keyword}{typename} traits::galerkin\_jacobian\_type;}
\DoxyCodeLine{CustomSolver<gal\_R\_type, gal\_J\_type> yourSolver(problem);}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// we assume a galState exists}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// you can take a single step of the problem}}
\DoxyCodeLine{\{}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} currentTime = 0.;}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.5;}
\DoxyCodeLine{  problem(galState, currentTime, dt, 1, yourSolver);}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can define your own stepping loop}}
\DoxyCodeLine{\{}
\DoxyCodeLine{  \textcolor{keywordtype}{double} currentTime = 0.;}
\DoxyCodeLine{  \textcolor{keyword}{const} \textcolor{keywordtype}{double} dt = 0.1;}
\DoxyCodeLine{  \textcolor{keywordflow}{for} (\textcolor{keywordtype}{int} step = 1; step <= 10; step++)\{}
\DoxyCodeLine{   problem(galState, currentTime, dt, 1, yourSolver);}
\DoxyCodeLine{   currentTime += dt;}
\DoxyCodeLine{  \}}
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// or you can use our own functions to advance the problem}}
\DoxyCodeLine{pode::advance\_n\_steps\_and\_observe(problem, \textcolor{comment}{/* args */}, yourSolver);}

\end{DoxyCode}




\begin{DoxyParagraph}{Pay attention to\+:}
If you need fine-\/grained access, i.\+e., you need to make the problem take a single step (in other words you don\textquotesingle{}t use our own {\ttfamily advance} methods), you should {\itshape always} just rely on {\ttfamily operator()}. To be more explicit, look at this snippet\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{comment}{// ...}}
\DoxyCodeLine{\textcolor{comment}{// assume we have a galState}}
\DoxyCodeLine{\textcolor{keyword}{auto} problem      = pgal::create\_default\_implicit\_problem(scheme, \textcolor{comment}{/* args */});}
\DoxyCodeLine{solver = \textcolor{comment}{// create solver somehow}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// !!! You should NOT do this, this is undefined behavior !!!}}
\DoxyCodeLine{solver.solve(problem, galState)}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// You should do this:}}
\DoxyCodeLine{problem(..., solver);}
\DoxyCodeLine{\textcolor{comment}{// or, better, rely on (the advance functions do the right thing behind the scenes)}}
\DoxyCodeLine{pode::advance\_n\_steps\_and\_observe(problem, \textcolor{comment}{/* args */}, solver);}

\end{DoxyCode}
 
\end{DoxyParagraph}


~\newline
 \DoxyHorRuler{0}
 ~\newline
\hypertarget{md_pages_components_rom_galerkin_autotoc_md97}{}\doxysection{Why does a Galerkin problem behave like a stepper?}\label{md_pages_components_rom_galerkin_autotoc_md97}
The answer is that practically speaking, at the lowest-\/level, a Galerkin problem can be reduced to simply a \char`\"{}custom\char`\"{} stepper to advance in time. This is how pressio implements this and the reason why a Galerkin problem contains a stepper object inside\+: when you create the problem, pressio creates the appropriate custom stepper object that you can use. You don\textquotesingle{}t need to know how this is done, or rely on the details, because these are problem-\/ and implementation-\/dependent, and we reserve the right to change this in the future. All you need to know is that a an explicit Galerkin problem behaves like a an explicit stepper, and an implicit Galerkin problem behaves like an implicit stepper. 