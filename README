#
# rompp Documentation
#

WARNING: WORK IN PROGRESS!  This code for ROM is based on TriBITS.
The project defines a TriBITS CMake project.

Example configure files are under sampleScripts.
More to come... 



Some utilities commands: 

(a) to replace string inside files 

	grep -rl 'timeIntegrator' . | xargs sed -i '' 's/timeIntegrator/ode/g'


(b) to rename files by replacing part of the string 

	rename-5.26 's/timeIntegrator/ode/' *.cc
