
#------------------------------------------------------------------------
# to include the lumped headers to have access to containers classes
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/../..)

ADD_SUBDIRECTORIES(
  playground
  #braceTest
  # kokkos_works
  #vector
  #eigen1
)
