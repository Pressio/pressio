

# function(get_file_list_no_ext TARGDIR RESULT)

	# TO finish

  # file(GLOB ${TARGIDR}/all_files_here "*")

  # foreach(f ${all_files_here})
  #   if(NOT f MATCHES "\\.txt"
  #       AND NOT f MATCHES ".DS_Store"
  #       AND NOT f MATCHES "${CMAKE_CURRENT_SOURCE_DIR}/[.].+")

  #     strip file estension
  #     string(REGEX REPLACE "\\.[^.]*$" "" f2 ${f})

  #   endif()
  # endforeach(f ${all_files_here})

# endfunction()
